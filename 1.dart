void main() {
// Great Learning Academy

  String title = 'Dart Course';
  String title1 = title[0].toUpperCase() + title.substring(1).toLowerCase();
  print(title1);
  print(title.toUpperCase());
  print(title.toLowerCase());
}
